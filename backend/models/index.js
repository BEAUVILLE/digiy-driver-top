// ========================================
// DIGIY DRIVER - Index des modèles
// ========================================

const User = require('./User');
const Driver = require('./Driver');
const Client = require('./Client');
const Ride = require('./Ride');

module.exports = {
    User,
    Driver,
    Client,
    Ride
};
